# Shared test environment for integration/load/E2E suites.
EMAIL_PROVIDER="mock"
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/merchtable_test?schema=public"

# Stripe test keys/secrets only.
STRIPE_SECRET_KEY="sk_test_mock"
STRIPE_WEBHOOK_SECRET="whsec_mock"

# Used by the mock provider payloads as the sender fallback.
SMTP_FROM="no-reply@example.com"
